const intialState={
    isAuthenticate:false,
    user:{},
    error:null,
}

export default function(state=intialState, action){
    switch(action.type){
        default:
            return state;
    }
}